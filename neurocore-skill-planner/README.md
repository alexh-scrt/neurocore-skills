# neurocore-skill-planner

Higher order aggregator skill that plans the task execution and engages other neurocore skills via neurogossip-agent-v3.

## Installation

```bash
pip install -e .
```

## Features

- Built as a standard NeuroCore skill.
- Implements `PlannerSkill`.
