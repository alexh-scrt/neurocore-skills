# neurocore-skill-coordinator

Higher order coordinator skill for NeuroCore. It coordinates high-level tasks by routing requests as per a YAML flow.

## Installation

```bash
pip install -e .
```

## Features

- Built as a standard NeuroCore skill.
- Implements `CoordinatorSkill`.
