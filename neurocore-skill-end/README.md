# neurocore-skill-end

Flow execution completion skill for NeuroCore. It stops execution and passes control back to the master agent.

## Installation

```bash
pip install -e .
```

## Features

- Built as a standard NeuroCore skill.
- Implements `EndSkill`.
