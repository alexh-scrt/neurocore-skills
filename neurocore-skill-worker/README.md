# neurocore-skill-worker

Higher order aggregator skill that coordinates with other tasks using neurogossip-agent-v3 via a passed YAML configuration.

## Installation

```bash
pip install -e .
```

## Features

- Built as a standard NeuroCore skill.
- Implements `WorkerSkill`.
